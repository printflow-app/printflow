{
  "name": "printflow-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate && npx prisma db push --accept-data-loss && npx ts-node prisma/seed.ts && npm run build",
    "start": "cd backend && npm run start:prod"
  }
}
