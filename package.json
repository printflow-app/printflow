{
  "name": "printflow-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate && npx prisma db push --accept-data-loss && npm run build",
    "start": "cd backend && npm run start:prod"
  }
}
